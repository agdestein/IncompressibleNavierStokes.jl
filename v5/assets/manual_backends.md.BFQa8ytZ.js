import{_ as s,o as a,c as i,an as t}from"./chunks/framework.DIuZ5ca0.js";const u=JSON.parse('{"title":"GPU support and floating point precision","description":"","frontmatter":{},"headers":[],"relativePath":"manual/backends.md","filePath":"manual/backends.md","lastUpdated":null}'),r={name:"manual/backends.md"};function o(n,e,l,p,c,d){return a(),i("div",null,[...e[0]||(e[0]=[t(`<h1 id="GPU-support-and-floating-point-precision" tabindex="-1">GPU support and floating point precision <a class="header-anchor" href="#GPU-support-and-floating-point-precision" aria-label="Permalink to &quot;GPU support and floating point precision {#GPU-support-and-floating-point-precision}&quot;">​</a></h1><h2 id="Backends" tabindex="-1">Backends <a class="header-anchor" href="#Backends" aria-label="Permalink to &quot;Backends {#Backends}&quot;">​</a></h2><p>All operators are matrix-free <a href="https://github.com/JuliaGPU/KernelAbstractions.jl/" target="_blank" rel="noreferrer">KernelAbstractions.jl</a> kernels, so the same code runs on different backends. The desired backend only has to be passed to the <a href="/IncompressibleNavierStokes.jl/v5/examples/generated/Actuator2D#Setup"><code>Setup</code></a> function:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> CUDA</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">setup </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> Setup</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(; kwargs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, backend </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> CUDABackend</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">())</span></span></code></pre></div><p>On the CPU (the default backend), the kernels are multithreaded if Julia is started with multiple threads (e.g. <code>julia -t auto</code>).</p><p>The package is developed and tested with CUDA-compatible GPUs. Other KernelAbstractions backends (<a href="https://github.com/JuliaGPU/AMDGPU.jl" target="_blank" rel="noreferrer">AMDGPU.jl</a>, <a href="https://github.com/JuliaGPU/Metal.jl" target="_blank" rel="noreferrer">Metal.jl</a>, <a href="https://github.com/JuliaGPU/oneAPI.jl" target="_blank" rel="noreferrer">oneAPI.jl</a>) are untested; the operators themselves are backend-agnostic, so the main constraint is the <a href="./pressure">pressure solver</a> — the spectral solvers need FFT plans for the given array type, and the direct solver needs a factorization (<code>psolver_cg</code> is fully matrix-free and should work anywhere).</p><div class="tip custom-block"><p class="custom-block-title"><code>psolver_direct</code> on CUDA</p><p>To use a specialized direct solver for CUDA, install and <code>using</code> both CUDA.jl and CUDSS.jl. Then <a href="/IncompressibleNavierStokes.jl/v5/manual/pressure#IncompressibleNavierStokes.psolver_direct-Tuple{Any}"><code>psolver_direct</code></a> will automatically use the CUDSS solver.</p></div><h2 id="Floating-point-precision" tabindex="-1">Floating point precision <a class="header-anchor" href="#Floating-point-precision" aria-label="Permalink to &quot;Floating point precision {#Floating-point-precision}&quot;">​</a></h2><p>IncompressibleNavierStokes generates efficient code for different floating point precisions, such as</p><ul><li><p>Double precision (<code>Float64</code>)</p></li><li><p>Single precision (<code>Float32</code>)</p></li><li><p>Half precision (<code>Float16</code>)</p></li></ul><p>To use single or half precision, all user input floats should be converted to the desired type, starting with the grid vectors in <a href="/IncompressibleNavierStokes.jl/v5/examples/generated/Actuator2D#Setup"><code>Setup</code></a>. Mixing different precisions causes unnecessary conversions and may break the code.</p><div class="tip custom-block"><p class="custom-block-title">GPU precision</p><p>For GPUs, single precision is preferred. <code>CUDA.jl</code>s <code>cu</code> converts to single precision.</p></div><div class="tip custom-block"><p class="custom-block-title">Pressure solvers</p><p><a href="https://github.com/JuliaSparse/SparseArrays.jl" target="_blank" rel="noreferrer"><code>SparseArrays.jl</code></a>s sparse matrix factorizations only support double precision, so <a href="/IncompressibleNavierStokes.jl/v5/manual/pressure#IncompressibleNavierStokes.psolver_direct-Tuple{Any}"><code>psolver_direct</code></a> only works for <code>Float64</code> on the CPU. Consider using an iterative solver such as <a href="/IncompressibleNavierStokes.jl/v5/manual/pressure#IncompressibleNavierStokes.psolver_cg-Tuple{Any}"><code>psolver_cg</code></a> when using single or half precision.</p></div>`,13)])])}const k=s(r,[["render",o]]);export{u as __pageData,k as default};
